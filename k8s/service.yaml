apiVersion: v1
kind: Service
metadata:
  name: golang-app-svc
  labels:
    app: golang-app
spec:
  type: NodePort
  selector:
    app: golang-app
  ports:
    - port: 80
      targetPort: 8080
      nodePort: 30080
